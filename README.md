# tree_content
A shell script that will let you generate a listing of all the files in a folder, and its subolfders, with their content. It can then be used in a LLM to defined the basis of an existing project before prompting.
